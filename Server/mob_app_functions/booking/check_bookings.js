setInterval(() => {
    const currentTime = new Date();
  
    console.log("Run");
    // Check for expired bookings
    // Update the booking status or perform any necessary actions
  
    // Check for approaching end times
    // Send SMS notifications to relevant customers
  }, 1000);